What happens if no name prop is passed to the Welcome component? Can you set a default value for the name prop?

1) Yes, you can set a default value via the defaultProps property

example:

Welcome.defaultProps = {
  name: "String"
};

